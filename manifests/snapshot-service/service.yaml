apiVersion: v1
kind: Service
metadata:
  name: py-wallet-snapshot-service
  labels:
    app: py-wallet-snapshot-service
spec:
  type: ClusterIP
  selector:
    app: py-wallet-snapshot-service
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
