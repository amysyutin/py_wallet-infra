.DS_Store
tmp/

# Sealed Secrets controller master key — offline only, never in git
.secret/
*master.key*
*sealed-secrets-master*
*.pem
tls.key

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
!.terraform.lock.hcl
