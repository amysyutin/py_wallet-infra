apiVersion: v1
kind: Service
metadata:
  name: py-wallet-front
  labels:
    app: py-wallet-front
spec:
  type: ClusterIP
  selector:
    app: py-wallet-front
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP