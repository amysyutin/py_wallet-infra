apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: py-wallet-tls
spec:
  secretName: py-wallet-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - pywallet.dev